name: Nathan

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to process 
        required: false
        type: string
      pr_selector:
        description: PR URL or branch name
        required: false
        type: string

jobs:
  gather-and-send:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."

  detect-n8n-response:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."

  receive-validate-and-comment:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
